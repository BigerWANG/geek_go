package test_condition

import "testing"

func TestCondition(t *testing.T) {
	/*
	if 条件 与其他语言的差异
	1， condition 表达式结果必须是布尔值
	2. 支持变量赋值, 支持两段写法, 当判断多返回值函数错误的时候比较有用
	if var, err := SomeFunc(); err==nil{
			....
			}else{
			do some error handle
			}
	 */



}

